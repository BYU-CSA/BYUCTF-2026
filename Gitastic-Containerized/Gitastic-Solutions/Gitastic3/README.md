# Gitastic 3
Description:
```markdown
With the increased growth of our startup we've decided that we should start tagging our code releases! However we accidentally tagged one with the wrong thing, can you find it?

git clone git://gitastic.csa.cyberjousting.com/challenge
```
**Author**: `Zinko`
## Writeup
This is to teach about git tags as an attribute in Git. You're able to easily grep through them.

They can run `git tag -l -n | grep "byuctf"` to search through the tags.


**Flag** - `byuctf{that's_a_lot_of_tags_wow}`
