# Intro

This uses the same binary as `intro`. Why do you think that had a win function in it ;)

Files:
- [intro](./intro)

### Writeup

Just format string it up. You should be able to either overwrite a got entry or the stack pointer pretty easily. I'm almost positive you can leak anything you need, too

(Solve script in progress probably)

Flag: `byuctf{%p_yourself}`